1. create the serve image
2. push to the dokce hub
3. use the docker compose file in the deployment server

change docker compose volume path

- `/home/ubuntu/kts:/opt/app`
- where is the compose files at

change the script/deploy.sh

- change what commands to run
